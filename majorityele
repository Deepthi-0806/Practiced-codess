class Solution {
public:
int majorityElement(vector<int>& nums) {
    unordered_map<int,int>trace;
    int n=nums.size();
    for(auto ch:nums)
    {
        trace[ch]++;
        if(trace[ch]>n/2)
        return ch;
    }
    return -1;
}
};
